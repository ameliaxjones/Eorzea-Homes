<%= form_with(model: housingsize) do |form| %>
  <% if housingsize.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(housingsize.errors.count, "error") %> prohibited this housingsize from being saved:</h2>

      <ul>
        <% housingsize.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
